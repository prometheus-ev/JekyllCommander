* deploy
  * configure web server
    * content negotiation
    * prefer-language
    * ForceLanguagePriority (?)
  * build _site
    * from specified tag (allows "rollback")
* preview
  * cf. "deploy" (from working copy)
* WYSIWYG editor (?)
* make files editable
  * _config.yml
  * atom.xml
  * css/*.css
  * javascripts/*.js
* git integration
  * commit/push ("save")
    * pull first! (handle conflicts)
  * tag ("publish")
    * pull first! (handle conflicts)
* `grep TODO .`
