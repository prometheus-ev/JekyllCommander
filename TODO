* deploy
  * configure web server
    * content negotiation
    * prefer-language
    * ForceLanguagePriority (?)
  * build _site from specified tag ("rollback")
* WYSIWYG editor (?)
* make files editable
  * _config.yml
  * atom.xml
  * css/*.css
  * javascripts/*.js
* git integration
  * handle conflicts!
* `grep TODO .`
