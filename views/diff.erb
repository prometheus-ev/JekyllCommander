<h2>Diff for <code><%= path_info %></code><%= " @ <code>#{@sha[0, 7]}</code>" if @sha %></h2>

<table class="diff">
<% for type, row in @diff %>
  <tr><td class="<%= type %>"><%= row %></td></tr>
<% end %>
</table>
